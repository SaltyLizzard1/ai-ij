anthropic>=1.0
beautifulsoup4>=4.12
Markdown>=3.5
Pillow>=10.0
# optional
# openai>=1.40        # LLM_PROVIDER=openai (captions only; vision tagging is Anthropic-only)
# pillow-heif>=0.16   # iPhone .heic photos
pytest>=8.0
